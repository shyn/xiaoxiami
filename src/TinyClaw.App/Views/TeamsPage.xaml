<Page x:Class="TinyClaw.App.Views.TeamsPage"
               xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
               xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
               xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
               Title="Teams">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="Teams" Style="{StaticResource TitleTextBlockStyle}" Margin="0,0,0,16" />

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
            <ui:Button Content="Refresh" Icon="{ui:SymbolIcon ArrowSync24}" Click="OnRefreshClick" />
        </StackPanel>

        <ItemsControl Grid.Row="2" ItemsSource="{Binding Teams}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <ui:CardControl Margin="4" Width="320" Padding="16">
                        <StackPanel>
                            <TextBlock Text="{Binding Name}" FontSize="18" FontWeight="Bold" />
                            <TextBlock Margin="0,4,0,0" Opacity="0.6">
                                <Run Text="ID:" />
                                <Run Text="{Binding Id, Mode=OneWay}" />
                            </TextBlock>
                            <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                <ui:SymbolIcon Symbol="Crown24" Margin="0,0,6,0" />
                                <TextBlock Text="Leader:" Margin="0,0,4,0" />
                                <TextBlock Text="{Binding LeaderAgent}" FontWeight="SemiBold" Foreground="Gold" />
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                <ui:SymbolIcon Symbol="People24" Margin="0,0,6,0" />
                                <TextBlock>
                                    <Run Text="{Binding MemberCount, Mode=OneWay}" />
                                    <Run Text=" members:" />
                                </TextBlock>
                            </StackPanel>
                            <TextBlock Text="{Binding Members}" TextWrapping="Wrap" Margin="28,2,0,0" Opacity="0.7" />
                        </StackPanel>
                    </ui:CardControl>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</Page>
