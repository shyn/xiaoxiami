<Page x:Class="TinyClaw.App.Views.AgentsPage"
               xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
               xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
               xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
               Title="Agents">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="Agents" Style="{StaticResource TitleTextBlockStyle}" Margin="0,0,0,16" />

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
            <ui:Button Content="Refresh" Icon="{ui:SymbolIcon ArrowSync24}" Click="OnRefreshClick" />
        </StackPanel>

        <ItemsControl Grid.Row="2" ItemsSource="{Binding Agents}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <ui:CardControl Margin="4" Width="320" Padding="16">
                        <StackPanel>
                            <TextBlock Text="{Binding Name}" FontSize="18" FontWeight="Bold" />
                            <TextBlock Margin="0,4,0,0" Opacity="0.6">
                                <Run Text="ID:" />
                                <Run Text="{Binding Id, Mode=OneWay}" />
                            </TextBlock>
                            <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                <ui:SymbolIcon Symbol="Cloud24" Margin="0,0,6,0" />
                                <TextBlock>
                                    <Run Text="{Binding Provider, Mode=OneWay}" />
                                    <Run Text=" / " />
                                    <Run Text="{Binding Model, Mode=OneWay}" />
                                </TextBlock>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                <ui:SymbolIcon Symbol="Folder24" Margin="0,0,6,0" />
                                <TextBlock Text="{Binding WorkingDirectory}" TextTrimming="CharacterEllipsis" MaxWidth="250" Opacity="0.7" />
                            </StackPanel>
                        </StackPanel>
                    </ui:CardControl>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</Page>
