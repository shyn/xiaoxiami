TELEGRAM_BOT_TOKEN=your-bot-token

# ── Model config ────────────────────────────────────────────────────
# All model/provider configuration lives in models.json (required).
# See models.json.example for the format.
#
# API keys can be set via environment variables and referenced
# in models.json with "apiKey": "env:ANTHROPIC_API_KEY"
#
# ANTHROPIC_API_KEY=sk-...
# OPENAI_API_KEY=sk-...

# ── Optional global thinking level override ─────────────────────────
# THINKING_LEVEL=medium

# ── Other ───────────────────────────────────────────────────────────
# AGENT_CWD=/path/to/working/directory
# TMUX_SOCKET_DIR=/tmp/pi-telegram-tmux
# AUTH_FILE=./auth.json
# DATA_DIR=.
# SESSION_DIR=./sessions
